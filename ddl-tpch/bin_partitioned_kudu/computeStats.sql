compute stats ${var:DB}.part;
compute stats ${var:DB}.partsupp;
compute stats ${var:DB}.supplier;
compute stats ${var:DB}.customer;
compute stats ${var:DB}.nation;
compute stats ${var:DB}.region;
compute stats ${var:DB}.orders;
compute stats ${var:DB}.lineitem;
